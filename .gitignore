# Environment
.env
.env.local
.env.*.local

# Dependencies
node_modules/
vendor/
venv/
__pycache__/

# Build outputs
dist/
build/
*.o
*.pyc

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
.venv

# SOREN runtime (locks, server logs)
.soren/logs/
.soren/run/

# SOREN runtime data — must survive git reset --hard rollback
# Journals (accumulated intelligence)
.soren/journal/
# Databases (agent registry, conversations, memories, auth)
.soren/*.db
.soren/*.db-shm
.soren/*.db-wal
# Agent registry JSON cache (regenerated from .db)
.soren/agent_registry.json
# Mailbox (ephemeral message queue)
.soren/mailbox
# Worker contexts (generated per-spawn, not source code)
.soren/worker-contexts/
# Daemon state files
.soren/.compact-timestamps
.soren/.compact-effectiveness
.soren/.supervisor-heartbeat
.soren/.sup-site-heartbeat
.soren/.last_healthy_commit
.soren/.router_line
.soren/.router_position
.soren/.log_markers/
.soren/.fix-retries/
.soren/.ui-check-flags/
.soren/.reminder-sent-ids
.soren/.auth-secret
# Send queue (runtime message delivery state)
.soren/send-queue/
# Mailbox archives (historical, large)
.soren/mailbox-archive/
.soren/mailbox.archive-*

# MCP config (contains secret tokens)
.mcp.json

# Encrypted secret vault (passphrase not stored; vault itself excluded from VCS)
.soren/secrets.enc
.soren/.secrets-salt

# SOREN runtime state (not tracked)
.soren/.heartbeat-*
.soren/.last-*
.soren/.sup-*
.soren/.tunnel.*
.soren/orchestrator.log*
.soren/server.pid
nohup.out

# One-time file→table migration artifacts (fix_retries/compact_timestamps/
# secrets_vault lazy imports rename the legacy files/dirs to *.migrated;
# *.importing.* is the transient atomic-claim name)
.soren/.fix-retries.migrated*
.soren/.compact-timestamps.migrated*
.soren/.compact-timestamps.importing.*
.soren/secrets.enc.migrated
.soren/.secrets-salt.migrated
.soren/.fix-retries.importing.*
.soren/schedule.json.migrated*
.soren/schedule.json.importing.*
.soren/preferences.json.migrated*
.soren/preferences.json.importing.*
.soren/prefs.json.migrated*
.soren/prefs.json.importing.*
# Transient tmp names used by the projects/teams view regenerations (tmp+mv)
.soren/projects.json.tmp.*
.soren/teams.json.tmp.*
